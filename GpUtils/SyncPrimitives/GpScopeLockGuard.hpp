#pragma once

#include <GpCore2/Config/GpConfig.hpp>

#if defined(GP_USE_SYNC_PRIMITIVES)

namespace GPlatform {

template<typename T>
class GpScopeLockGuard
{
public:
    ?

private:
    ?
};

}// namespace GPlatform

#endif// #if defined(GP_USE_SYNC_PRIMITIVES)
